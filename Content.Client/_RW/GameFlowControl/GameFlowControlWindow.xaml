<controls:FancyWindow xmlns="https://spacestation14.io"
                      xmlns:gfx="clr-namespace:Robust.Client.Graphics;assembly=Robust.Client"
                      xmlns:controls="clr-namespace:Content.Client.UserInterface.Controls;assembly=Content.Client"
                      Title="{Loc 'game-flow-control-window-title'}"
                      SetSize="850 650"
                      MinSize="750 550">
    <BoxContainer Orientation="Vertical" Margin="8" SeparationOverride="8">
        <!-- Control Header / Occupier Banner -->
        <PanelContainer>
            <PanelContainer.PanelOverride>
                <gfx:StyleBoxFlat BackgroundColor="#2D2D35" BorderThickness="1" BorderColor="#4A4A56"/>
            </PanelContainer.PanelOverride>
            <BoxContainer Orientation="Horizontal" Margin="6">
                <Label Name="OccupierLabel" StyleClasses="LabelHeading" Text="{Loc 'game-flow-control-unoccupied-label'}" HorizontalExpand="True" VerticalAlignment="Center"/>
                <Button Name="ReleaseBtn" Text="{Loc 'game-flow-control-release-control'}" VerticalAlignment="Center"/>
            </BoxContainer>
        </PanelContainer>

        <BoxContainer Orientation="Horizontal" SeparationOverride="10" VerticalExpand="True">
            <!-- Left Panel (Scheduler configs, Chaos, and History) -->
            <BoxContainer Orientation="Vertical" HorizontalExpand="True" SeparationOverride="8" MinWidth="380">
                
                <!-- Interval settings -->
                <controls:StripeBack>
                    <Label Text="{Loc 'game-flow-control-interval-title'}" StyleClasses="LabelHeading" Margin="6 4"/>
                </controls:StripeBack>
                
                <BoxContainer Orientation="Vertical" SeparationOverride="6" Margin="4">
                    <Label Name="SchedulerTypeLabel" Text="Active Scheduler: None" StyleClasses="LabelSubheading"/>
                    <GridContainer Columns="2" HSeparationOverride="6" VSeparationOverride="6">
                        <Label Text="{Loc 'game-flow-control-min-interval'}"/>
                        <SpinBox Name="MinIntervalSpin" HorizontalExpand="True"/>
                        
                        <Label Text="{Loc 'game-flow-control-max-interval'}"/>
                        <SpinBox Name="MaxIntervalSpin" HorizontalExpand="True"/>
                        
                        <Label Text="{Loc 'game-flow-control-time-left'}"/>
                        <SpinBox Name="TimeLeftSpin" HorizontalExpand="True"/>
                    </GridContainer>
                    <Button Name="ApplyIntervalBtn" Text="{Loc 'game-flow-control-apply-settings'}" HorizontalAlignment="Center"/>
                </BoxContainer>

                <!-- Chaos score settings -->
                <BoxContainer Name="ChaosSection" Orientation="Vertical" SeparationOverride="6">
                    <controls:StripeBack>
                        <Label Text="{Loc 'game-flow-control-chaos-title'}" StyleClasses="LabelHeading" Margin="6 4"/>
                    </controls:StripeBack>
                    
                    <BoxContainer Orientation="Vertical" SeparationOverride="6" Margin="4">
                        <GridContainer Columns="2" HSeparationOverride="6" VSeparationOverride="6">
                            <Label Text="{Loc 'game-flow-control-chaos-score'}"/>
                            <SpinBox Name="ChaosScoreSpin" HorizontalExpand="True"/>
                        </GridContainer>
                        <Button Name="ApplyChaosBtn" Text="{Loc 'game-flow-control-apply-chaos'}" HorizontalAlignment="Center"/>
                    </BoxContainer>
                </BoxContainer>

                <!-- History -->
                <controls:StripeBack>
                    <Label Text="{Loc 'game-flow-control-history-title'}" StyleClasses="LabelHeading" Margin="6 4"/>
                </controls:StripeBack>
                
                <PanelContainer VerticalExpand="True">
                    <PanelContainer.PanelOverride>
                        <gfx:StyleBoxFlat BackgroundColor="#1E1E22" BorderThickness="1" BorderColor="#3A3A46"/>
                    </PanelContainer.PanelOverride>
                    <ScrollContainer HorizontalExpand="True" VerticalExpand="True" HScrollEnabled="False">
                        <BoxContainer Name="HistoryContainer" Orientation="Vertical" Margin="6" SeparationOverride="4"/>
                    </ScrollContainer>
                </PanelContainer>

            </BoxContainer>

            <!-- Right Panel (Pending approvals & trigger rules list) -->
            <BoxContainer Orientation="Vertical" HorizontalExpand="True" SeparationOverride="8">
                
                <!-- Pending approvals -->
                <controls:StripeBack>
                    <Label Text="{Loc 'game-flow-control-pending-title'}" StyleClasses="LabelHeading" Margin="6 4"/>
                </controls:StripeBack>
                
                <PanelContainer MinHeight="150">
                    <PanelContainer.PanelOverride>
                        <gfx:StyleBoxFlat BackgroundColor="#221E1E" BorderThickness="1" BorderColor="#463A3A"/>
                    </PanelContainer.PanelOverride>
                    <ScrollContainer HorizontalExpand="True" VerticalExpand="True" HScrollEnabled="False">
                        <BoxContainer Name="PendingContainer" Orientation="Vertical" Margin="6" SeparationOverride="4"/>
                    </ScrollContainer>
                </PanelContainer>

                <!-- Trigger events list -->
                <controls:StripeBack>
                    <Label Text="Rules Spawning" StyleClasses="LabelHeading" Margin="6 4"/>
                </controls:StripeBack>
                
                <LineEdit Name="SearchEdit" PlaceHolder="{Loc 'game-flow-control-search-placeholder'}"/>
                
                <PanelContainer VerticalExpand="True">
                    <PanelContainer.PanelOverride>
                        <gfx:StyleBoxFlat BackgroundColor="#1E1E22" BorderThickness="1" BorderColor="#3A3A46"/>
                    </PanelContainer.PanelOverride>
                    <ItemList Name="RulesList" VerticalExpand="True"/>
                </PanelContainer>
                
                <Button Name="RunRuleBtn" Text="{Loc 'game-flow-control-run-rule'}" HorizontalAlignment="Center"/>

            </BoxContainer>
        </BoxContainer>
    </BoxContainer>
</controls:FancyWindow>
