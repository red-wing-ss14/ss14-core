<controls:FancyWindow xmlns="https://spacestation14.io"
                      xmlns:gfx="clr-namespace:Robust.Client.Graphics;assembly=Robust.Client"
                      xmlns:controls="clr-namespace:Content.Client.UserInterface.Controls;assembly=Content.Client"
                      Title="{Loc 'brainrot-panel-title'}"
                      SetSize="450 550"
                      MinSize="350 450">
    <BoxContainer Orientation="Vertical" Margin="10" SeparationOverride="8">
        <!-- Input section -->
        <BoxContainer Orientation="Horizontal" SeparationOverride="6">
            <LineEdit Name="AddInput" PlaceHolder="{Loc 'brainrot-panel-placeholder'}" HorizontalExpand="True"/>
            <Button Name="AddBtn" Text="{Loc 'brainrot-panel-add'}"/>
        </BoxContainer>

        <controls:StripeBack>
            <Label Text="{Loc 'brainrot-panel-heading'}" StyleClasses="LabelHeading" Margin="6 4"/>
        </controls:StripeBack>

        <!-- List Section -->
        <PanelContainer VerticalExpand="True">
            <PanelContainer.PanelOverride>
                <gfx:StyleBoxFlat BackgroundColor="#1E1E22" BorderThickness="1" BorderColor="#3A3A46"/>
            </PanelContainer.PanelOverride>
            <ScrollContainer VerticalExpand="True" HScrollEnabled="False">
                <BoxContainer Name="TriggersContainer" Orientation="Vertical" Margin="6" SeparationOverride="4"/>
            </ScrollContainer>
        </PanelContainer>
    </BoxContainer>
</controls:FancyWindow>
